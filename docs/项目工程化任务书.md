# 项目工程化任务书

> 版本：v14.0  
> 目标：从现有项目逆向分析 → 重新设计数据库 → 反向生成代码改动  
> 执行策略：分阶段确认 + 分块处理（大文件友好）  
> 执行模式：批量执行（阶段 3 不中断）  
> 验证方式：人工验证核心功能（数据持久化 + 数据流转）  
> 三方服务：简化版（名称/用途/付费/配置/风险）  
> 表前缀：用户填写

---

## 📋 项目前置信息

### 基础信息

| 项目 | 内容 | 来源 | 示例 |
|------|------|------|------|
| 项目路径 | （待提供） | 👤 用户填写 | `D:\work\ailpd` |
| 前端开发服务器端口 | （待提供） | 👤 用户填写 | `4001` |
| 后端 API 端口 | （待提供） | 👤 用户填写 | `5001` |
| 基础路径 | （待提供） | 👤 用户填写 | `/interviewer/` 或 `/` |
| 数据库表前缀 | （待提供） | 👤 用户填写 | `ailpd_`（格式：`xxx_`） |

> **基础路径说明**：前端应用部署的 URL 路径前缀。如果直接部署在域名根目录，填 `/`；如果部署在子目录，填 `/子目录名/`  
> **数据库表前缀说明**：所有生成的 MySQL 表名会加上此前缀，格式必须为 `xxx_`（末尾带下划线），如 `ailpd_user_info`

### 技术栈

| 技术 | 版本 | 用途 | 所属 |
|------|------|------|------|
| React | 19.2.4 | UI 框架 | 前端 |
| TypeScript | 5.8.2 | 类型安全 | 前端 |
| Vite | 6.2.0 | 构建工具 | 前端 |
| Node.js | ESM 模块 | 运行时环境 | 后端 |
| Express | 4.18.2 | Web 框架 | 后端 |
| MySQL2 | 3.6.5 | 数据库驱动 | 后端 |
| CORS | 2.8.5 | 跨域支持 | 后端 |
| dotenv | 16.3.1 | 环境变量管理 | 后端 |
| body-parser | 1.20.2 | 请求体解析 | 后端 |

**进程管理：**
- PM2 集群模式部署
- 最大内存重启：500M
- 多实例运行

**数据库：**
- MySQL（关系型数据库）

---

## 🎯 执行策略

### 阶段划分
```
阶段 1（分析）：01-04 文档 → 🫵 用户确认 → 继续
阶段 2（设计）：05 文档     → 🫵 用户确认 → 继续
阶段 3（执行）：06-07 文档   → ✅ 完成
```

### 大文件保障机制

| 风险 | 解决方案 |
|------|----------|
| 文件太大超时 | 分块读取/写入（每块 < 5000 字） |
| 上下文超长 | 中间结果持久化到临时文件 |
| 分析遗漏 | 子代理独立分析 + 交叉验证 |
| 执行失败 | 每任务可独立回滚 |
| 进度丢失 | 每阶段完成后保存状态（支持断点续传） |

### 技术要求
| 指标 | 限制 |
|------|------|
| 单文件处理时间 | < 30 秒 |
| 单任务执行时间 | < 60 秒 |
| 单次写入内容 | < 5000 字 |
| 每任务修改文件数 | ≤ 3 个 |

---

## 第一阶段：项目分析

### 1. 三方服务清单（简化版）

**1.1** 扫描项目使用的三方服务，输出到：
```
docs/01-三方服务清单.md
```

**内容要求（简化）：**
| 字段 | 说明 |
|------|------|
| 服务名称 | 如 Google AI SDK |
| 用途 | 如 AI 对话生成 |
| 是否付费 | 是/否 |
| 配置位置 | 如 .env 中的 `GOOGLE_API_KEY` |
| 风险等级 | 高/中/低（Key 泄露/服务停用/费用风险） |

**1.2** QA 检查：
- 方法：扫描 package.json + 代码 import + .env 配置
- 标准：三者交叉验证，无遗漏
- 输出：在文档末尾注明「已知局限」

---

### 2. 项目结构关系图

**2.1** 分析项目目录结构、模块依赖、调用关系，输出到：
```
docs/02-项目结构关系图.md
```

**2.2** QA 检查：
- 方法：子代理独立分析（spawn 另一个代理重新扫描）
- 标准：对比两次分析结果，差异项人工确认
- 输出：在文档末尾注明「已知局限」

---

### 3. 项目数据流转图

**3.1** 分析数据从输入 → 处理 → 存储 → 输出的完整流转路径，输出到：
```
docs/03-项目数据流转图.md
```

**3.2** QA 检查：
- 方法：抽样验证（选取 5 个核心功能，追踪数据流）
- 标准：核心数据流无遗漏，边缘数据流注明局限
- 输出：在文档末尾注明「已知局限」

---

### 4. 功能点清单

**4.1** 提取所有功能点，包含：
- **显性功能**：代码中直接可见的功能（API、页面、按钮等）
- **隐性功能**：通过数据流转、业务逻辑推断的功能（定时任务、数据同步、缓存策略等）

输出到：
```
docs/04-功能点清单.md
```

**4.2** QA 检查：
- 方法：代码调用链验证 + 路由/接口扫描
- 标准：每个功能点可追溯到代码位置
- 输出：在文档末尾注明「已知局限」

---

## 第二阶段：持久化方案设计

### 5. MySQL 持久化存储方案

**5.1** 结合【docs/04-功能点清单.md】和【docs/03-项目数据流转图.md】，全面分析功能点和数据流转之间的关系，设计 MySQL 持久化方案，输出到：
```
docs/05-MySQL 持久化存储方案.md
```

**5.2** QA 检查：
- 方法：验证每张表与功能点的映射关系（每表至少对应一个功能点）
- 标准：无孤立表、无遗漏功能点
- 输出：在文档末尾注明「已知局限」

**5.3** 审计字段规范（每张表必须包含）：

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `create_user_id` | char(36) | 创建者 UserId |
| `create_fullname` | varchar(255) | 创建者真实姓名 |
| `create_time` | datetime(3) | 创建时间 |
| `update_user_id` | char(36) | 最新修改者 UserId |
| `update_fullname` | varchar(255) | 最后更新者真实姓名 |
| `update_time` | datetime(3) | 最新修改时间 |

**5.4** 表命名规范：
- 所有表名前缀：使用用户填写的「数据库表前缀」（格式：`xxx_`）
- 命名风格：小写 + 下划线（如 `ailpd_user_info`）

---

## 第三阶段：任务执行

### 前置准备（阶段 3 开始前）

**P.1** 确认执行范围：

**P.1** 确认执行范围：
- [ ] 修改现有代码（数据库模型、API、前端组件）
- [ ] 生成新代码（新增文件）
- [ ] 创建备份（git 分支或文件备份）
- [ ] 确认回滚方案

---

### 6. 执行任务清单

**6.1** 基于【docs/05-MySQL 持久化存储方案.md】生成执行任务清单：
- 每个任务修改文件数：**≤ 3 个**
- 任务之间可独立执行
- 输出到：`docs/06-执行任务清单.md`

**6.2** QA 检查：
- 方法：验证任务覆盖所有表和功能点
- 标准：每个表至少有一个任务，每个功能点可追溯
- 输出：在文档末尾注明「已知局限」

---

### 7. 任务执行

**7.1** 基于【docs/06-执行任务清单.md】执行任务：
- 按顺序执行每个任务
- 记录每个任务的执行结果（成功/失败/异常信息）
- 输出到：`docs/07-任务执行结果.md`

**7.2** 执行后验证：
- 人工验证核心功能
- 检查数据是否正确保存到数据库
- 验证数据流转是否正确
- 如有问题，记录原因和修复方案

**技术要求**：
- 大文件处理：分块读取、分块写入
- 避免超时：单任务执行时间 < 60 秒
- 控制上下文：单次写入内容 < 5000 字

---

## 📁 输出目录结构

```
docs/
├── 01-三方服务清单.md
├── 02-项目结构关系图.md
├── 03-项目数据流转图.md
├── 04-功能点清单.md
├── 05-MySQL 持久化存储方案.md
├── 06-执行任务清单.md
└── 07-任务执行结果.md
```

---

## ✅ 验收标准

| 文档 | 验收标准 | 阶段确认点 |
|------|----------|------------|
| 01-三方服务清单 | 扫描代码 + 依赖文件 + .env 配置，三者交叉验证 | ✅ 阶段 1 |
| 02-项目结构关系图 | 子代理独立分析，差异项已确认 | ✅ 阶段 1 |
| 03-项目数据流转图 | 核心数据流可追溯，边缘数据流已注明 | ✅ 阶段 1 |
| 04-功能点清单 | 每个功能点可追溯到代码位置 | ✅ 阶段 1 |
| 05-MySQL 持久化存储方案 | 每表对应功能点，无孤立表 | ✅ 阶段 2 |
| 06-执行任务清单 | 每任务≤3 文件，覆盖所有表和功能点 | ⏭️ 阶段 3 内部 |
| 07-任务执行结果 | 所有任务有明确结果，核心功能人工验证通过 | ✅ 阶段 3 |

---

## 📊 状态管理

### 进度追踪
```
docs/.progress.json - 记录执行进度和状态（支持断点续传）
```

### 状态格式示例
```json
{
  "currentStage": 1,
  "completedDocs": ["01", "02"],
  "pendingConfirmation": true,
  "lastUpdated": "2026-03-25T10:00:00Z"
}
```

### 阶段完成标志
| 阶段 | 完成条件 |
|------|----------|
| 阶段 1 | 01-04 文档生成 + QA 完成 + 用户确认 |
| 阶段 2 | 05 文档生成 + QA 完成 + 用户确认 |
| 阶段 3 | 06-07 文档生成 + 核心功能人工验证通过 |

---

## ⚠️ 注意事项

| # | 事项 | 说明 |
|---|------|------|
| 1 | **大文件处理** | 所有读写操作使用分块方式，避免超时 |
| 2 | **上下文控制** | 单次写入内容 < 5000 字，超长时分多文件 |
| 3 | **备份优先** | 执行任何修改前先备份 |
| 4 | **可回滚** | 每个任务可独立回滚 |
| 5 | **进度同步** | 每阶段完成后同步进度，等待确认 |

---

## 📝 修订记录

| 版本 | 日期 | 修改内容 | 修改人 |
|------|------|----------|--------|
| v1.0 | 2026-03-25 | 初始版本 | 肖刚 |
| v2.0 | 2026-03-25 | 增加执行策略、大文件保障机制、状态管理 | AI 助理 |
| v3.0 | 2026-03-25 | 增加附录 | AI 助理 |
| v4.0 | 2026-03-25 | 删除附录、删除「是否可停机」字段 | AI 助理 |
| v5.0 | 2026-03-25 | 整合完整技术栈信息 | AI 助理 |
| v6.0 | 2026-03-25 | 合并前端后端技术栈为单一表格 | AI 助理 |
| v7.0 | 2026-03-25 | 删除「代码规模」字段 | AI 助理 |
| v8.0 | 2026-03-25 | 端口和基础路径改为用户填写 | AI 助理 |
| v9.0 | 2026-03-25 | 增加填写示例和基础路径说明 | AI 助理 |
| v10.0 | 2026-03-25 | 统一版本号、调整启动流程、增加前置准备章节 | AI 助理 |
| v11.0 | 2026-03-25 | 删除测试用例章节，改为人工验证核心功能 | AI 助理 |
| v12.0 | 2026-03-25 | 文件名改为「项目工程化任务书」 | AI 助理 |
| v13.0 | 2026-03-25 | 三方服务清单简化为 5 个字段 | AI 助理 |
| v14.0 | 2026-03-25 | 数据库表前缀改为用户填写 | AI 助理 |

---

## 🚀 启动流程

```
步骤 1：用户填写前置信息（项目路径、端口、基础路径、表前缀）
         ↓
步骤 2：用户发送「开始阶段 1」
         ↓
步骤 3：执行阶段 1 → 🫵 用户确认 → 阶段 2 → 🫵 用户确认 → 阶段 3（批量执行 + 人工验证）→ ✅ 完成
```

---

## ⚠️ 重要提醒

| 原则 | 说明 |
|------|------|
| **备份优先** | 执行任何修改前先备份（git 分支或文件复制） |
| **可回滚** | 每个任务可独立回滚，失败不影响已完成部分 |
| **进度同步** | 每阶段完成后同步进度，等待用户确认 |
| **异常处理** | 遇到失败记录原因，提供修复方案或回滚选项 |
| **透明执行** | 所有操作可追溯，关键决策有记录 |
